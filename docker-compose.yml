services:
  postgres:
    image: postgres:17
    container_name: postgres
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: kalaro_orders
    ports:
      - "5432:5432"
    volumes:
      - ./data:/var/lib/postgresql/data